// 2

let userChoice = +prompt("How many siblings do you have?")
if (userChoice === 1) {
    console.log("1 siblon");
} else if (userChoice > 1) {
    console.log("numSiblings is g1reater than1");
} else {
    console.log("No siblings");
}

// the type === we need and + convert string to number